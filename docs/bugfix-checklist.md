# 已确认问题修复清单

2026-09-26 更新。本清单记录修复、复现依据和验证边界；本地改动尚未发布。

## APPROVAL-01：长审批内容与窄气泡布局（待办）

- [ ] 用户要求先记为待办，本轮未改动审批弹窗布局。
- 现象：Caelis 命令审批把完整多行脚本放进标题；标题和动作重复显示，360 pt 固定宽度的
  桌宠气泡形成很长的粗体文本，难以核对动作、对象和授权范围。
- 原因：`internal/backend/caelis/projection.go` 将原生 `tool_call.title` 同时映射为
  `Title` 和 `Action`；`frontend/src/Panel.tsx` 直接渲染标题；原生 `bot_bubble_height`
  每次仍固定窗口宽度为 360 pt。
- 待实施：简短动作标题，独立展示对象、理由和一次性授权范围；完整命令只在可读、可滚动
  的详情区展示，避免重复。长审批可使用更宽的详情表面；决定按钮始终可达，沿用精确原生请求与选项。
- 验收：覆盖多行脚本、长路径、文件变更、多个审批、中文/英文，以及桌宠和完整聊天；
  浅深背景、屏幕边缘和键盘操作需要原生验收，截图不能替代决定回执验证。

## APPROVAL-02：Caelis 实时审批未进入可见状态（已修复，尚未发布）

- [x] 已修复实时事件触发的精确审批核对；独立于 APPROVAL-01 的布局待办。
- 现象：模型已说正在等待审批，但用户暂时看不到审批；数分钟后审批才出现。
- 原因：`applyEnvelope` 未消费 `session/request_permission`，带 `approval_request_id`
  的审批解决事件也提前返回。`watch` 仍推进游标；`needsRefreshLocked` 在订阅健康时
  不因这些事件触发权威状态核对。审批只能随 bootstrap、续租或其他维护读取状态补入。
- 隔离验证：通过临时 Go overlay 向生产投影注入合成事件，不访问日常运行时、不提交审批。
  到达事件后 `approvals=0, phase=working, maintenance_refresh=false`；匹配的解决事件后
  `approvals=1, maintenance_refresh=false`。两个期望及时同步的断言均失败；直接补入权威
  `SessionState.approval` 的对照能正常显示。这是修复前证据，非通过的验收。
- 时序依据：2026-09-25 第二次清理审批在 22:23:41 已由 Host 发出，模型在 22:26:52
  输出等待说明，用户在 22:28:58 批准。原生连接租期 10 分钟，Bot 提前 3 分钟续租；
  健康连接可约每 7 分钟才因续租读取状态，与分钟级迟到吻合。未记录前端首次可见时间，
  不把后端事件时间当成弹窗实际显示时间。
- 修复方向：审批到达、替换与解决驱动及时投影或精确状态核对；不能仅缩短轮询间隔。
  保留请求/目标身份、队列顺序、过期与未知结果语义；不能从助手文字创建审批或自动授权。
- 验收：健康长订阅期间主 Bot/worker 审批都及时出现；到达、替换、解决、乱序状态读取、
  重连和长命令均覆盖；审批解决不能终结所属回合，也不能重复授权。

## APPROVAL-03：批准后命令继续执行，主 Bot 不再跟进（可选增强，尚未发布）

- [x] Bot 保留审批命令的跟进记录，确认原生终态后发送一次完成通知；结果通过原 Task 读取。
- [x] 配套 Caelis 修复：应用自有会话的只读终端输出权限、应用来源透传、生产者已退出但
  Task 仍记 running 时终端快照错误。能力标识为 `application-terminal-observation-v1`。
  当前正式 v0.62.0 未包含此能力，Bot 仍正常连接但不启用此项跟进；其他修复无需等待它。
  Caelis 独立提 PR，不单独发 release。只读观察不改变普通会话的自动续跑策略。
- 已验证：隔离真实 Host + 本地合成模型执行迟到审批，命令恰好执行一次，自动通知使模型
  继续处理。没有修改日常 Store、批准用户原清理命令或替换已安装 Runtime。
- 真实记录：第一次命令返回 `waiting_approval` 后，模型两次 `Task wait` 仍未获批准，
  22:13:57 输出最终等待说明；22:14:45 用户批准，命令启动，原回合随即结束。
  22:21:45 是另一个受管 worker 的完成通知唤醒了 Bot，新回合才读取此前命令结果。
  不能将这次继续解释为命令审批后的自动汇报。
- Caelis 的异步命令 continuation 保留命令执行，但已返回的工具观察者不再接收最终结果；
  final safe point 等待 continuation 并不等于再次请求模型核对命令结果。批准只代表当前
  命令可以执行，不代表命令完成、用户目标完成或结果已经汇报。
- 修复方向：专业长任务优先走 Bot 受管任务；仍允许主 Bot 发起的异步命令必须有明确的
  后续观察/交付责任。待审批、执行中、结果待汇报分别显示，不能依赖其他 worker 或用户
  再发一句话碰巧唤醒。不得用重新执行命令或盲目重发 prompt 实现恢复。
- 验收：模型已输出 final 后用户迟到批准/拒绝，命令立即完成/长期运行/失败，以及没有
  其他 worker 的场景，都能保留真实状态并最终汇报；不重复执行、不空闲轮询模型。

## 本次委派核对与工作区需求（2026-09-25）

本次“排查 caelis-bot 可清理分支”实际通过 `bot_task_start` 创建，记录存在于产品任务账本，
并使用独立 `Tasks/<handle>` 工作目录；没有用原生 spawn 创建这项审计工作。日志里的
`Task read/wait` 是 Caelis 对异步命令的读取/等待工具，不是 Bot 任务创建工具。
Caelis 仓库清理则由主 Bot 直接执行，未创建对应受管任务。

- [x] `bot_task_start.workspace` 支持已有绝对目录；Codex 与 Caelis 都保留实际 cwd 和原生权限。
  不提供 workspace 时继续分配私有目录；重试不改变工作区，也不重新 pin 用户移除的任务。
- [x] 新任务有空位自动 pin（最多 8 项）；满额不挤掉旧项，Bot 英文 skill 指导主动整理。
- [x] 终端小球的开关能力已评估，具体窗口身份与各终端能力适配尚未实施，见
  [任务委派](task-delegation.md#终端小球开关评估2026-09-25待实施)。
- 英文核心 skill 和任务参考已同步工作区、优先受管任务、pin 管理和迟到审批结果核对。

## CHAT-01：发送后用户消息延迟出现，等待圆点先出现

- [x] 实现修复并通过回归。
- 现象：提交用户输入后，聊天先显示 Bot 的等待圆点，用户消息稍后才出现。
- 原因：`frontend/src/Panel.tsx` 的 Composer 提交时不添加待发送消息，聊天依赖后端
  `userMessage` 投影；Codex adapter 在等待 `turn/start` 回执前已发布 `sending`，
  `chatActivity` 据此显示圆点。450 ms 的快照轮询还会增加显示延迟。
- 已验证：修复前的临时 Go overlay 用例阻塞发送回执，生产 Session 快照为 `phase=sending`，
  用户消息数仍为 0。此处是修复前证据，不代表当前实现。
- 修复方向：提交时立即显示带发送状态的用户消息，以稳定请求标识与原生回执/消息合并；
  明确区分发送中、已接受、拒绝和结果未知，不以本地显示冒充后端接受。
- 验收：慢回执时用户消息先于等待圆点可见；接受后不重复、不闪退；拒绝或未知时可理解、
  保留草稿且不自动重发；覆盖普通消息、附件和运行中补充，以及聊天/快捷输入表面切换。

## CHAT-02：worker 已完成后仍显示忙碌和停止按钮

- [x] 实现修复并通过回归。
- 现象：主对话与两个 worker 均有真实完成记录，聊天仍显示等待圆点和停止按钮。
- 已验证：本机原生记录确认本次两个子回合与主回合均已完成。当前日志未保留原始 App Server
  通知顺序，不能据此声称完整重建了现场事件顺序。
- 可复现缺陷：`internal/backend/codex/bot.go` 的 `childEvent` 收到子回合
  `turn/completed` 后删除活动记录；若随后收到主回合的 `subAgentActivity(kind=completed)`，
  `trackWorkerActivity` 又写入空 run 的 `childRuns` 条目，并因 `childWatching=true`
  直接返回。`Session.update` 因残留条目将完成状态改回 `working`。
- 修复前临时 Go overlay 用例固定两种顺序：先活动通知、后子回合终态时正常；先子回合终态、
  后活动通知时得到 `working / canSend=false / canInterrupt=true`，完成态断言失败。
- 修复方向：原生回合终态不能被无回合标识的迟到活动通知重新激活；必要的只读核对应与
  长期订阅状态分开，仍保留真正的新回合和未知结果，不根据助手文本推断完成。
- 验收：覆盖两种事件顺序、重复完成通知、订阅恢复与新回合；主回合和全部相关子回合结束后
  消除虚假忙碌、恢复输入与角色空闲，真正运行中的 worker 和审批仍受正确管理。

## 关联调查：本次 worker 为什么没有脚底终端入口

本次模型调用的是 Codex 原生 `spawn_agent` / `wait_agent`，未调用 Bot 的 `bot_task_start`。
两个子代理各有独立 thread、原生父子关系及完成记录，但共用 Bot 的 Notebook 工作目录；
不属于 Bot 创建的独立 `Tasks/<handle>` 工作任务。

本机记录仅包含 native `children`，adapter 的受管任务集合与 `tasks.json` 产品账本均为空。
`WorkStates` 只导出受管任务，`TaskPreviews` 只从产品账本生成脚底入口，`WorkTerminal`
也要求受管任务绑定。因此这次没有脚底入口，不能解释为任务太快完成或没有真实执行。
此外，当前原生 UI 在聊天窗口获得焦点时会暂时隐藏任务入口；这与账本为空是独立条件。

现有产品约定要求专业工作经 Bot 自有任务接口进入受管目录。后续需要核对秘书工具暴露与
委派约束为何仍允许走原生子代理路径；本轮只记录调查结果，未改变委派策略或接管已有线程。


## 本轮实现和验收（2026-09-25）

- CHAT-01：renderer 在等待草稿持久化前插入 pending 气泡，host outbox 支持表面切换；
  Codex clientUserMessageId、Caelis prompt 回执的精确回合目标及 steer 的 input_operation_id
  与原生消息合并，重复文本不作为关联依据。Caelis 在新 prompt 回执待定期间保留原生消息，
  等待精确关联后才展示，避免真实消息先到导致重复；该状态随操作日志恢复。
  覆盖慢回执、附件名称、接受/拒绝/未知、同回合追加、迟到原生消息及顺序保持。
  outbox 是进程内展示状态；重启后的执行真相由原生记录恢复，不重放发送。
- CHAT-02：父活动通知不再重新激活已订阅 child；迟到同一回合 started 也不能覆盖终态。
  回归覆盖两种事件顺序、重复通知、真正新回合，现有订阅/重连测试继续通过。
- [x] 聊天/快捷输入均使用后端发送或追加能力；原生 exact-turn steering 保持不变。
- [x] Codex 委派 prompt 不再添加多重 JSON 请求/通用限制包装；worker 固定文案精简。
- [x] 英文核心 skill 及四个 references 整目录打包；应用 instructions 只有 metadata/位置。
- [x] TaskPreview 携带原生状态，收起与展开显示 loading，终态及时停止；隐藏/减少动态效果适配。
- [x] AppKit 安装 contentView 前设置目标尺寸，修复气泡展开和预览裁切。

实际 React 组件以合成回执在浏览器中验收：运行中输入切换为“补充说明”；回执未返回时
用户气泡已出现且位于 Bot 圆点之前；接受后同一气泡去除发送状态、无重复；未知保留草稿且
不可自动重发。快捷输入追加接受后清空草稿。此证明不等同于原生 Wails 输入法/跨窗口时序验收。

原生 AppKit fixture 检查并捕获实际展开气泡：窗口和内容尺寸一致，按钮不越界，状态更新
不移动悬停目标，loading/终态/隐藏/减少动态效果通过。完整桌宠场景的跨屏 hover 和 Terminal
点击仍保留人工验收项。Codex/Caelis 安装版 + 本机合成 provider 已验证技能元数据与逐步
原生文件读取以及 worker 隔离；Caelis 原生运行中连续追加相同文本的两条消息，各自按请求 ID
独立展示。没有调用真实付费模型。技能长期自主遵循/压缩恢复另行观察。
