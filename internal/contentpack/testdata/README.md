# Basic content fixture

`basic.glb` is a twelve-vertex geometric character created for this repository,
licensed under the root Apache-2.0 license. It has no production-character art,
external resources, skeleton, animation or custom metadata. It is intentionally
small so creators and tests can exercise the basic contract independently of the
private authoring repository. See `docs/content-packs.md` for the creator workflow.
