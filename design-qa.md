# 桌宠输入与消息设计 QA — 2026-09-19

final result: passed for the observed native message/input/decision/detail surfaces; remaining hardware and backend gates are recorded in docs/backend-acceptance.md.

以下先保留 P1 胶囊历史记录，最新 P2 变更见文末。

## 目标与参照

本轮按用户反馈将输入与设置分开。用户提供的当前截图显示旧版 + 混入尺寸、重置、
隐藏及常驻连接说明；这些内容已移出。仍保留角色下方紧凑输入、外部点击收起、无关闭 X。

- 当前折叠截图：`docs/verification/p1-polished-capsule.jpg`，420×64 logical points。
- 当前展开截图：`docs/verification/p1-polished-attachments.jpg`，420×117 logical points。
- 旧版 `p1-plus-menu.png`、`p1-capsule.png` 和 `p1-card-before.png` 仅保留为迭代历史。
- 本轮用户截图与运行中的附件菜单截图已共同查看；按相对构图比较，不冒充等像素复制。

## 检查与调整

- 胶囊只保留 + / 输入 / 发送三段；无标题、问候、连接说明、关闭 X 或设置。
- + 只展开添加文件。选择后收起并恢复输入焦点，附件标签可单独移除。
  没有真实插件时不放置假的引用列表；后续插件引用仍归 +，配置归独立原生菜单项。
- 配置在角色右键和状态栏菜单内。大小仅用原生连续滑杆，65%–160%，显示百分比取整，
  保存任意精度；菜单不跟着宠物缩放而移动。不再有预设/重置/跳转或右键打开输入框。
- 胶囊 31px 圆角、32px 圆形按钮，系统字体 15/13px。系统 SF Symbols 图标沿用，
  状态栏改用首页 favicon 原样副本，未重画品牌图标。
- 原生 sheet 选择、标签移除和 Esc 已实机复核，用户确认原生菜单正常。发送仍禁用，
  不用常驻技术说明填满输入表面；真实错误才按需显示。

## 限制

火柴人仍为 GLB 占位。真实对话、插件引用、结果和附件发送待 P2。右键/状态栏的视觉
截图未由自动化获得；菜单正常的结论来自用户真实鼠标反馈。拖动换成 Window Server
之后，用户已确认“明显更跟手，松手和点击都正常”；详见原生验收。
视觉通过仍不替代多屏/桌面场景验收。

## 多行输入追加

Shift+Enter 后编辑区按内容增高，最大 127px（约五行）后滚动；按钮贴编辑区底部，
不随每一行居中跳动。原生两行与七行内容已查看，清空可回到 420×64，
截图见 `docs/verification/p1-multiline-input.jpg`。Enter 已绑定发送按钮，当前按钮仍禁用。

## P2 最新消息气泡与决定面板

- 用户的新参考要求实时内容在宠物上方，输入在发送后消失。实际实现为 360×96 非激活
  原生气泡；只保留当前请求最新助手输出/工具进度，没有默认完整 transcript。
- 后端接受后清空相应草稿并关闭输入；真实发送后 trace 证实 panelVisible=false、
  bubbleVisible=true、bubbleKey=false，前台回到原应用。用户确认位置正常。
- 深色半透明材质、轻描边、两行消息摘要和小型查看/停止按钮；长输出通过独立详情阅读。
  不伪造语音、插件或动画入口。辅助功能按钮包含最新消息文本。
- 审批单独显示动作、位置与明确选择。一次性允许和取消直接可见；规则/会话授权
  需展开“更多授权方式”，显示范围与后续影响。真实打印审批一次性允许已通过；原生确认后自动收起。
  操作内容从顶部阅读，不沿用聊天历史的自动滚到底部行为。
- 气泡被点击才取用户意图，原生输入/决定面板才取键盘焦点。拖动中暂时隐藏气泡，
  松手/缩放后重新定位；不建立逐帧追赶窗口。顶部避让仍需更多边缘/多屏场景验收。
- [实际气泡](docs/verification/p2-message-bubble.png)、
  [实际审批](docs/verification/p2-approval-panel.png)、
  [独立历史](docs/verification/p2-history-window.png)。
  `p2-native-file-result.png` 是拆分气泡之前的文件全路径证据，不能作为最终布局截图。


## 2026-09-19 当前 Bot 表面

- 统一语义配色 + prefers-color-scheme；原生窗口背景使用系统动态颜色。
- 输入只在空闲展开，保留 + /文本/发送；接受后收起，共享草稿跨聊天入口连续。
- 气泡只呈现最新助手/必要状态，运行可停止，完成可 ✓；审批在同一气泡原位展开。
- 聊天为 IM 用户/助手布局，可发送/补充、停止、审批、打开文件结果；无默认工具流水和新会话。
- 原生实际截图：[深色聊天](docs/verification/p2-bot-chat-dark.png)、
  [深色气泡](docs/verification/p2-bot-bubble-dark.png)。旧截图保留为历史，不能代表当前主题。
- 已实测同气泡一次性审批、✓/重启确认保持、原生聊天中文发送、菜单打开聊天与草稿延续。
  未在本轮改变用户系统外观设置；浅色/动态切换仅有实现，组合实机验收仍待补。
- 聊天仍为纯文本；大量历史增量加载、完整无障碍与多显示器/Space 组合回归待补。
